export const environment = {
  // Empty = same-origin. In dev the Angular dev-server proxy (proxy.conf.json)
  // forwards /api (and the /api WebSocket) to the backend on :8080, so the
  // browser never makes a cross-origin request and CORS never applies.
  apiBaseUrl: '',
};
