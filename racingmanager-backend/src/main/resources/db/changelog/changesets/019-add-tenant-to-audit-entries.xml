<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="add-tenant-to-audit-entries" author="ragin">
        <preConditions onFail="MARK_RAN">
            <not><columnExists tableName="audit_entries" columnName="tenant_id"/></not>
        </preConditions>
        <!-- Nullable at the DB level: the startup DefaultTenantMigration backfills
             existing rows before any other startup code reads them. -->
        <addColumn tableName="audit_entries">
            <column name="tenant_id" type="BINARY(16)">
                <constraints foreignKeyName="fk_audit_entries_tenant" references="tenants(id)"/>
            </column>
        </addColumn>
    </changeSet>
</databaseChangeLog>
