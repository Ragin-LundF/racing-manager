# Agent Instructions

All project context and canonical AI instructions live in **`.ai/README.md`** —
read it first. It holds the project overview, module/stack details, common
commands, and the progressive-loading index for `.ai/instructions`, `.ai/skills`,
and `.ai/harness`.

Load only the documents relevant to the current task, and follow the conflict-
resolution order defined in `.ai/README.md`.
